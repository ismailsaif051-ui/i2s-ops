'use client';

import { ErrorScreen } from '@/components/error-screen';

// Erreurs du cadre lui-même — typiquement la session, lue en premier par le
// layout de l'application, quand le service redémarre.
export default ErrorScreen;
